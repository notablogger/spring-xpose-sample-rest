name: PR Build

on:
  pull_request:
    branches: [ main ]
    types: [ opened, synchronize, reopened, ready_for_review ]

concurrency:
  group: pr-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  branch-lint:
    name: Branch name
    runs-on: ubuntu-latest
    steps:
      - name: Validate branch name
        env:
          BRANCH: ${{ github.head_ref }}
        run: |
          echo "Branch: $BRANCH"
          if ! echo "$BRANCH" | grep -qE '^(feat|fix|docs|test|chore|refactor|ci)/[a-z0-9][a-z0-9\-]*[a-z0-9]$'; then
            echo "::error::Branch '$BRANCH' does not follow the required pattern."
            echo "::error::Expected: <type>/<kebab-case-description>"
            echo "::error::Types: feat, fix, docs, test, chore, refactor, ci"
            echo "::error::Example: feat/add-secured-orders-entity"
            exit 1
          fi

  title-lint:
    name: PR title
    runs-on: ubuntu-latest
    steps:
      - name: Validate PR title follows Conventional Commits
        env:
          TITLE: ${{ github.event.pull_request.title }}
        run: |
          echo "PR title: $TITLE"
          if ! echo "$TITLE" | grep -qE '^(feat|fix|docs|test|chore|refactor|ci|perf|revert)(\([a-z]+\))?: .{1,72}$'; then
            echo "::error::PR title '$TITLE' does not follow Conventional Commits."
            echo "::error::Format: type(scope): short description"
            echo "::error::Example: feat(sample): add pageable product listing"
            exit 1
          fi

  test:
    name: Test
    runs-on: ubuntu-latest
    needs: [ branch-lint, title-lint ]

    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: temurin

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v3
        with:
          cache-read-only: true

      - name: Run tests
        run: ./gradlew test --no-daemon

      - name: Upload test reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: pr-test-reports
          path: build/reports/tests/
          retention-days: 3

  build-check:
    name: Build check — bootJar + Docker image
    runs-on: ubuntu-latest
    needs: test

    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: temurin

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v3
        with:
          cache-read-only: true

      - name: Build bootJar
        run: ./gradlew bootJar -x test --no-daemon

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build Docker image (no push)
        uses: docker/build-push-action@v6
        with:
          context: .
          push: false
          tags: notablogger/spring-xpose-sample-rest:pr-${{ github.event.pull_request.number }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  pr-gate:
    name: PR gate (all checks passed)
    runs-on: ubuntu-latest
    needs: [ branch-lint, title-lint, test, build-check ]
    if: always()
    steps:
      - name: Check all jobs succeeded
        run: |
          results='${{ toJSON(needs) }}'
          echo "$results"
          if echo "$results" | grep -q '"result": "failure"'; then
            echo "::error::One or more required checks failed."
            exit 1
          fi
          echo "All required checks passed ✅"
